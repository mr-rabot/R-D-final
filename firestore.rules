
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Completely open rules for development and to resolve permission blockers
    // Note: Since we are moving to root folder logic for data, these are fallbacks.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
